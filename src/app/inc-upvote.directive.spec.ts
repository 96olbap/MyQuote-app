import { IncUpvoteDirective } from './inc-upvote.directive';

describe('IncUpvoteDirective', () => {
  it('should create an instance', () => {
    const directive = new IncUpvoteDirective();
    expect(directive).toBeTruthy();
  });
});
